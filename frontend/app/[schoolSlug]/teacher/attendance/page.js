// Canonical spelling. Keep the implementation in the legacy module temporarily
// so existing bookmarks continue to render while the API workflow is connected.
export { default } from "../attendence/page";
