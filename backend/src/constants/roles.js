const ROLES = {
  STUDENT: 'student',
  TEACHER: 'teacher',
  FINANCE_MANAGER: 'finance_manager',
  ADMIN: 'admin',
};

const ALL_ROLES = Object.values(ROLES);

module.exports = {
  ROLES,
  ALL_ROLES,
};
